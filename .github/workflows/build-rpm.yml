name: Build RPM

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-rpm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install RPM tools
        run: sudo apt-get update && sudo apt-get install -y rpm

      - name: Get version
        id: version
        run: echo "version=$(grep -oP '__version__ = "\K[^"]+' src/fedora_l10n/__init__.py)" >> $GITHUB_OUTPUT

      - name: Setup RPM build
        run: |
          mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          VERSION=${{ steps.version.outputs.version }}
          DIR=fedora-l10n-$VERSION
          mkdir -p $DIR/src/fedora_l10n
          mkdir -p $DIR/data/icons $DIR/data/man
          cp src/fedora_l10n/*.py $DIR/src/fedora_l10n/
          cp data/se.danielnylander.FedoraL10n.desktop $DIR/data/
          cp data/icons/se.danielnylander.fedora-l10n.svg $DIR/data/icons/
          cp data/man/fedora-l10n.1 $DIR/data/man/
          cp LICENSE $DIR/
          test -f po/sv/LC_MESSAGES/fedora-l10n.mo && { mkdir -p $DIR/po/sv/LC_MESSAGES; cp po/sv/LC_MESSAGES/fedora-l10n.mo $DIR/po/sv/LC_MESSAGES/; } || true
          tar czf ~/rpmbuild/SOURCES/$DIR.tar.gz $DIR

      - name: Create spec file
        run: |
          cat > ~/rpmbuild/SPECS/fedora-l10n.spec << 'SPEC'
          Name:           fedora-l10n
          Version:        VERSION_PLACEHOLDER
          Release:        1
          Summary:        Fedora Linux translation status viewer
          License:        GPL-3.0-or-later
          URL:            https://github.com/yeager/fedora-l10n
          Source0:        %{name}-%{version}.tar.gz
          BuildArch:      noarch
          Requires:       python3 >= 3.9
          Requires:       python3-gobject
          Requires:       gtk4
          Requires:       libadwaita
          Requires:       python3-requests

          %description
          A GTK4/Libadwaita application for viewing Fedora Linux translation progress.

          %prep
          %setup -q

          %install
          mkdir -p %{buildroot}%{_datadir}/fedora-l10n/fedora_l10n
          cp src/fedora_l10n/*.py %{buildroot}%{_datadir}/fedora-l10n/fedora_l10n/
          mkdir -p %{buildroot}%{_bindir}
          cat > %{buildroot}%{_bindir}/fedora-l10n << 'WRAPPER'
          #!/bin/bash
          exec python3 -c "import sys; sys.path.insert(0, '/usr/share/fedora-l10n'); from fedora_l10n.main import main; main()" "$@"
          WRAPPER
          chmod 755 %{buildroot}%{_bindir}/fedora-l10n
          install -Dm644 data/se.danielnylander.FedoraL10n.desktop %{buildroot}%{_datadir}/applications/se.danielnylander.FedoraL10n.desktop
          install -Dm644 data/icons/se.danielnylander.fedora-l10n.svg %{buildroot}%{_datadir}/icons/hicolor/scalable/apps/se.danielnylander.fedora-l10n.svg
          install -Dm644 data/man/fedora-l10n.1 %{buildroot}%{_mandir}/man1/fedora-l10n.1
          test -f po/sv/LC_MESSAGES/fedora-l10n.mo && install -Dm644 po/sv/LC_MESSAGES/fedora-l10n.mo %{buildroot}%{_datadir}/locale/sv/LC_MESSAGES/fedora-l10n.mo || true

          %files
          %license LICENSE
          %{_bindir}/fedora-l10n
          %{_datadir}/fedora-l10n/
          %{_datadir}/applications/se.danielnylander.FedoraL10n.desktop
          %{_datadir}/icons/hicolor/scalable/apps/se.danielnylander.fedora-l10n.svg
          %{_mandir}/man1/fedora-l10n.1*
          %{_datadir}/locale/sv/LC_MESSAGES/fedora-l10n.mo

          %changelog
          * $(date "+%a %b %d %Y") Daniel Nylander <daniel@danielnylander.se> - VERSION_PLACEHOLDER-1
          - RPM package
          SPEC
          sed -i "s/VERSION_PLACEHOLDER/${{ steps.version.outputs.version }}/g" ~/rpmbuild/SPECS/fedora-l10n.spec

      - name: Build RPM
        run: rpmbuild -ba ~/rpmbuild/SPECS/fedora-l10n.spec

      - name: Upload RPM to release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${{ steps.version.outputs.version }}
          gh release upload "v$VERSION" \
            ~/rpmbuild/RPMS/noarch/fedora-l10n-${VERSION}-1.noarch.rpm \
            --clobber
